# Google AI Studio key (free tier) — https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Anthropic key — used when the UI toggle "Use Claude (Anthropic)" is on,
# or with `python solution.py --provider anthropic`. https://platform.claude.com
ANTHROPIC_API_KEY=
